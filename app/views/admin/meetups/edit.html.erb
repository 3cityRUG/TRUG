<div class="admin-form">
  <div class="page-header">
    <h1>Edytuj spotkanie #<%= @meetup.number %></h1>
    <div class="header-actions">
      <%= link_to "← Wróć do spotkania", admin_meetup_path(@meetup), class: "btn btn-secondary" %>
    </div>
  </div>

  <%= form_with model: [:admin, @meetup], local: true do |f| %>
    <% if @meetup.errors.any? %>
      <div class="form-errors">
        <ul>
          <% @meetup.errors.each do |error| %>
            <li><%= error.full_message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>

    <div class="form-group">
      <%= f.label :number, "Numer spotkania" %>
      <%= f.number_field :number, required: true %>
    </div>

    <div class="form-group">
      <%= f.label :date, "Data" %>
      <%= f.date_field :date, required: true %>
    </div>

    <div class="form-group">
      <%= f.label :description, "Opis" %>
      <%= f.text_area :description %>
    </div>

    <div class="form-actions">
      <%= f.submit "Zapisz zmiany", class: "btn btn-primary" %>
      <%= link_to "Anuluj", admin_meetups_path, class: "btn btn-secondary" %>
    </div>
  <% end %>
</div>
