#!/bin/bash -e

# Always run migrations on startup
./bin/rails db:migrate

# If running the rails server then prepare the database (only if fresh)
# if [ "${@: -2:1}" == "./bin/rails" ] && [ "${@: -1:1}" == "server" ]; then
#   ./bin/rails db:prepare
# fi

exec "${@}"
